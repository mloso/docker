version: "3.8"

services:
  backend:
    image: backend-app
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
  frontend:
    image: frontend-app
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "5000:3000"
